# Client-side (safe to expose in the browser — protected by Row Level Security)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (used by Vercel serverless functions in /api — NEVER prefix with VITE_)
SUPABASE_SERVICE_ROLE_KEY=
GEMINI_API_KEY=

# Optional: max AI generations per business per day (used by the AI proxy's rate limiter)
AI_DAILY_LIMIT_PER_BUSINESS=50
